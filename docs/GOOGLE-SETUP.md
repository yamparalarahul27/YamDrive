# Google setup is no longer required

Pitstop now uses MapLibre, OpenStreetMap and Valhalla for in-app maps and routing.
The former Google Maps/Places key instructions no longer apply. Existing keys in
`.env` are not read by the app. See the main README for the current setup.
